# recskit's core has zero required dependencies -- it's pure Python.

# Optional, only needed if you want the Sage 50 adapter's connection
# helper (recskit works fine without it if you supply ledger data from
# somewhere else, or wire your own cursor up to recskit.sage_adapter):
# sagekit @ git+https://github.com/FredScriptsPT/sagekit.git

# Optional, only needed to run the tests:
# pytest>=7.0
